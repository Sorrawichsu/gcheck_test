[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "gcheck"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "google-genai",
    "typer",
    "rich",
    "python-dotenv",
]

[project.scripts]
gcheck = "main:app"
